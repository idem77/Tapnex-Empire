<resources>

    <!-- Light Theme -->
    <style name="Theme.TapnexEmpire.Light" parent="Theme.Material3.Light">
        <item name="android:windowBackground">@color/LightCream</item>
    </style>

    <!-- Dark Theme (optional) -->
    <style name="Theme.TapnexEmpire.Dark" parent="Theme.Material3.Dark">
        <item name="android:windowBackground">@color/CharcoalBlack</item>
    </style>

</resources>
